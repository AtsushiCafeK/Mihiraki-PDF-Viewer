==Mihiraki 1.0.9 README========================================

==概要==========================================================
Mihiraki（ミヒラキ）は、PDFを“本のように”閲覧することを目的にした、シンプルなPDFビューアーです。
表紙は1ページ表示、2ページ目以降は見開き表示に切り替えて読めます。
さらに、ローカルLLM（Ollama）を使ってPDFの内容（テキスト＋画像）を要約する機能も備えています。

==主な機能======================================================
・PDFをタブで複数同時表示（複数ファイルを開けます）
・ドラッグ＆ドロップでPDFを開く
・ファイルの関連付け（Mihiraki を既定のPDFアプリに設定可能）
・ページ移動（スライダー＋前/次ボタン＋キーボード左右キー）
・拡大縮小（Zoom +/-）
・表示フィット（Fit Page / Fit Width）
・ドラッグでパン（表示中のページを掴んで移動）
・縦スクロールモード（全ページを縦に並べてスクロールで閲覧）
・目次（TOC）表示／目次からのページ移動（PDF内に目次がある場合）
・見開き表示（表紙は単ページ、2ページ目以降は見開き）
・右綴じ/左綴じ切り替え（RTL切替で左右配置を反転）
・PNG書き出し（全ページをPNGファイルに一括変換）
・表示設定の記憶（スクロール/見開き/RTL/ズームを次回起動時に復元）
・Ollamaによる要約（テキスト＋画像を含めた要約に対応）

==動作環境======================================================
・Windows 10 / 11 推奨
・Python 3.10+（開発環境によって差がありますが、3.10〜3.12で動作しやすいです）
・Ollama（要約機能を使う場合）

==起動方法======================================================

Mihiraki.extを起動、インストールは不要です。

==操作方法======================================================

PDFを開く
・ツールバーの「Open」からPDFを選択してください。
・PDFファイルをウィンドウにドラッグ＆ドロップしても開けます。
・Mihirakiを既定のPDFアプリに設定すると、ファイルをダブルクリックして開けます。
開いたPDFはタブとして追加されます。

ページ移動
・スライダーを動かす
・左（◀）/右（▶）ボタンで移動
・キーボードの左右キー（← →）で移動
・ページ表示エリアの左端・右端クリックで移動

拡大縮小
・「Zoom +」で拡大
・「Zoom -」で縮小

フィット表示
・「Fit Page」：ページ全体が入るように表示
・「Fit Width」：横幅に合わせて表示

縦スクロールモード
・「Scroll」をONにすると全ページが縦に並んで表示されます
・マウスホイールやスクロールバーでページをスクロールして閲覧できます
・ページ番号はスクロール位置に合わせて自動で更新されます
・◀▶ボタン・スライダー・キーボードキーでのページジャンプも引き続き使えます
・ウィンドウをリサイズすると表示幅に合わせて自動で再レイアウトされます

目次（TOC）
・「TOC」をONにすると目次ドックが表示されます
・項目をクリックすると該当ページへ移動します
※PDFに目次情報が無い場合は表示されないことがあります。

見開き表示（本のように読む）
・「Spread」をONにすると見開き表示になります
・表紙（1ページ目）は単ページ、2ページ目から見開きになります
・最後のページが余る場合は単ページになります

右綴じ/左綴じ切り替え（見開きの左右）
・「RTL」をONにすると見開きの左右配置が反転します
・右開き（日本の本）/左開き（洋書）を切り替える用途です
※ページ順そのものは変えず、左右の配置のみを入れ替えます。

PNG書き出し
・ツールバーの「Export PNG」をクリックしてください
・保存先フォルダを選ぶと、全ページを「0001.png」「0002.png」…の形式で書き出します

表示設定の記憶
スクロール/見開き/RTL/RTL Nav/ズームの設定は自動で保存され、次回起動時・次のPDFを開いたときに復元されます。

==Ollamaによる要約================================================
MihirakiはローカルLLMとしてOllamaを利用し、PDFの内容を要約できます。
要約は「テキスト抽出＋ページ画像（Vision）」を組み合わせて行います。

==事前準備（Ollama）==============================================

Ollamaのインストール
Ollamaをインストールし、起動してください。

使用モデル（このバージョンの想定）
Gemma3:12b を使用します（コードをデフォルト値としてあります）。
モデルは変更が可能です。使うモデルの名称を入力してください。

例：モデル取得（未取得の場合）
ollama pull gemma3:12b
または、ollamaアプリを起動して取得。

Ollamaが起動していることの確認
既定では http://localhost:11434
 で待ち受けます。

==要約の使い方====================================================

PDFを開く

ツールバーの「Summarize」をクリック

ダイアログで以下を指定
・Ollama host：通常は http://localhost:11434

・Model：gemma3:12b
・ページ範囲：全ページまたは現在ページのみ等
・画像も含めて要約：ON推奨（Vision向け）
・バッチ（ページ/回）：重い場合は小さめにすると安定します（例：2〜4）

「要約開始」を押す
進捗が表示され、完了すると要約結果が表示されます。

==要約がうまくいかない場合========================================
・Ollamaが起動しているか確認してください。
・hostが正しいか確認してください（既定：http://localhost:11434）。

・モデル名（gemma3:12b）が正しいか確認してください。
・ページ数が多いPDFは、ページ範囲を絞ると安定します。
・画像要約（Vision）が重い場合は「画像も含めて要約」をOFFにすると軽くなります。
・ファイアウォールやプロキシ環境でlocalhost通信が制限されていないか確認してください。

==既知の制限======================================================
・PDFの内容やフォント、画像によっては表示が完全に一致しない場合があります。
・目次（TOC）はPDFに埋め込まれている場合のみ利用できます。
・Ollama要約はモデル性能・PDF品質に依存します。根拠のない断定を含む可能性があるため、重要な判断は原文と突き合わせてください。

==ライセンス======================================================
MITライセンス（別添付ライセンスファイルに準ずる）

==謝辞============================================================
・Qt / PySide6
・PDFium / pypdfium2
・PyMuPDF
・Ollama
・その他OSSコミュニティ

==更新履歴========================================================
1.0.8
・縦スクロールモードの追加（Scrollボタン）
・表示設定の記憶（スクロール/見開き/RTL/ズームを次回起動時に復元）
・ドラッグ＆ドロップでPDFを開く機能の追加
・コマンドライン引数対応（既定のアプリとして登録可能）
・PNG書き出し機能の追加（Export PNG）
・フォント描画品質の向上

1.0.6
・メニューの説明をツールチップ機能で追加

1.0.5
・RTL機能にRTL Nav機能の追加
・キーボード左右キーでのページ操作追加
・ウィンドウ左右端をクリックでページ操作追加

1.0.3
・アプリ名「Mihiraki」へ統一
・見開き表示（表紙単ページ＋2ページ目以降見開き）
・右綴じ/左綴じ（RTL切替）
・OllamaによるPDF要約（テキスト＋画像）

==問い合わせ======================================================
不具合報告・改善提案は、配布ページまたはプロジェクトの案内に従ってください。